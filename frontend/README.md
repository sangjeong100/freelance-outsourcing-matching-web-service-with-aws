# init

```
$ npm install react
$ npm i react-bootstrap (bootstrap 사용)
$ npm i react-router-dom (react router 사용)
$ npm i axios 
$ npm i react-dom
$ npm i jquery
$ npm i jquery.cookie 
$ npm i ckeditor4-react (글쓰기 form)
$ npm i ckeditor4-react-advanced
$ npm i moment (날짜 형식)
$ npm install semantic-ui-react semantic-ui-css
$ npm install -g npm
$ npm install --save react-images-upload
$ npm install emailjs-com (email 서비스)
$ npm install react-paginate --save
$ npm install --save-dev @testing-library/react @testing-library/jest-dom (테스트 코드 사용)
$ npm install pm2 -g
```

## 실행
```
$npm start
```
